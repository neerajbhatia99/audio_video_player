import 'package:flutter/material.dart';
import 'package:mymediaplayer/audioplayer.dart';
import 'mainscreen.dart';


void main() => runApp(MyApp());


class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MainScreen();
  }
}